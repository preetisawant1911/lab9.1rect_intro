 # React + Vite Component Library

A simple and clean component library built with **React**, **TypeScript**, and **Vite**.  
This project demonstrates reusable UI components with a professional folder structure.

---

## � Features
- ⚛️ Built with React 18 + TypeScript
- ⚡ Fast development using Vite
- � TailwindCSS styling (if configured)
- � Reusable components: `UserProfileCard`, `AlertBox`, `ProductDisplay`
- � Clean folder structure with `.gitignore` to exclude `node_modules`

---

## � Folder Structure
